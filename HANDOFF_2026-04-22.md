# World Cup Predictions Handoff

Date: 2026-04-22
Branch target: `master`
Repo: `https://github.com/MoranTheKing/worldcup-predictions`

---

## Phase 1.5: League Server Actions + /game Prediction Hub

Implemented the full league create/join flow and restructured the UI to strictly separate
Tournament Info from the Prediction Game.

### New Routes

- `/game` → redirects to `/game/leagues`
- `/game/leagues` → My Leagues hub (create/join + list)
- `/game/leagues/[id]` → Individual league view
- `app/game/layout.tsx` — DashboardShell wrapper

### Server Actions: `app/actions/league.ts`

- `createLeague` — inserts into `leagues` + `league_members`, redirects to league page
- `joinLeague` — admin-client lookup by `invite_code`, inserts member, redirects
- Both use `try/catch` and return typed `LeagueActionState` errors

---

## Phase 2: League Debug + Full Predictions Hub

Date: 2026-04-22

### Bug Fix: Create League Failing Silently

Root cause: `leagues.owner_id` had TWO foreign key constraints:
1. Original init FK → `public.users(id)` (old table)
2. Phase 1 FK → `public.profiles(id)` (new table)

New users (Google OAuth) may not exist in `public.users`, causing the insert to fail
violating the original FK. The error was swallowed by the generic error path.

Fixes applied:
- **Migration `20260422000011_fix_leagues_owner_fk.sql`**: drops `leagues_owner_id_fkey`
  (old FK to `users`), backfills `public.users` from `auth.users`, ensures the Phase-1
  profiles FK exists.
- **`app/actions/league.ts` rewrite**: full `try/catch` on every DB call, `console.error`
  with the actual error message, admin client used for the `leagues` insert (bypasses RLS
  edge-cases while auth is still verified via `getUser()`).

### New Architecture: /game Sub-Navigation

`components/game/GameSubNav.tsx` — pill-tab sub-nav with two sections:
- **הליגות שלי** (`/game/leagues`) — social play hub
- **הניחושים שלי** (`/game/predictions`) — match betting & outrights

`app/game/layout.tsx` — updated to render `GameSubNav` above children, with shared page
header (title + subtitle).

### My Leagues UI (`/game/leagues`)

- `app/game/leagues/page.tsx` — server component; fetches `league_members → leagues`
- `app/game/leagues/LeaguesClient.tsx` — client component with:
  - Create League form (name input, `useActionState`, spinner)
  - Join League form (invite code input, `useActionState`, spinner)
  - My Leagues list (card links to each league, shows invite code)
  - Unauthenticated state handled gracefully

### League View (`/game/leagues/[id]`)

- Leaderboard table: `league_members → profiles(display_name, total_score)` sorted by
  `total_score DESC`
- Medal emoji for top 3 positions (🥇🥈🥉)
- Self-highlight row in neon tint
- Invite code display card

### My Predictions UI (`/game/predictions`)

#### Match Betting

- `app/game/predictions/page.tsx` — server: fetches `matches WHERE status='scheduled'`,
  teams (via `attachTeamsToMatches`), and existing `predictions` for the user
- `app/game/predictions/MatchPredictionCard.tsx` — client card per match:
  - Home/away team logos, Hebrew names, stage label, date/time
  - Two score number inputs (home, away), existing prediction pre-filled as `defaultValue`
  - `useActionState` with `upsertMatchPrediction.bind(null, matchId)` pattern
  - Per-card spinner, inline error, "✓ הניחוש נשמר" success feedback
- `app/game/predictions/PredictionsClient.tsx` — renders all cards + gates unauthenticated users

#### Tournament Outrights

- `app/game/predictions/OutrightForm.tsx` — client form:
  - `<select>` dropdown for winner team (all teams, sorted by `name_he`)
  - Text input for top scorer
  - Pre-populated from existing `tournament_predictions` row
  - `useActionState` with `upsertTournamentPrediction`

### Server Actions: `app/actions/predictions.ts`

`upsertMatchPrediction(matchId, _prev, formData)`:
- Validates scores (non-negative integers)
- Upserts into `predictions` table with `onConflict: "user_id,match_id"`

`upsertTournamentPrediction(_prev, formData)`:
- Upserts into `tournament_predictions` (new scoring table)
- Mirrors to `outright_bets` (backward compat with ProfileClient)
- `predicted_winner_team_id` cast to integer (teams.id is BIGINT)

---

## What Remains (Phase 3+)

- Scoring / points-award logic (award points when match status → finished)
- Automatic score recalculation trigger after match updates
- Prediction locking (stop accepting predictions when match goes live)
- Full invite/join UX (copy-to-clipboard, QR share)
- Profile nickname sync → `profiles.display_name` on all edit paths
- Mobile viewport QA for connector spacing in knockout bracket
- Fill the missing 495th key in `fifa_2026_matchups.json`

---

## Files Changed in Phase 2

- `supabase/migrations/20260422000011_fix_leagues_owner_fk.sql` (new)
- `app/actions/league.ts` (rewritten)
- `app/actions/predictions.ts` (new)
- `components/game/GameSubNav.tsx` (new)
- `app/game/layout.tsx` (updated — sub-nav + shared header)
- `app/game/page.tsx` (updated — redirect to /game/leagues)
- `app/game/leagues/page.tsx` (new)
- `app/game/leagues/LeaguesClient.tsx` (new)
- `app/game/leagues/[id]/page.tsx` (rewritten — scored leaderboard)
- `app/game/predictions/page.tsx` (new)
- `app/game/predictions/PredictionsClient.tsx` (new)
- `app/game/predictions/MatchPredictionCard.tsx` (new)
- `app/game/predictions/OutrightForm.tsx` (new)
- `HANDOFF_2026-04-22.md` (updated)


## Phase 2.1 - Security Hardening + League Management UX

Date: 2026-04-22

This pass tightened both the DB model and the `/game` UX after the first Phase 2 rollout.

### Security / DB

- added `supabase/migrations/20260422000014_harden_game_social_security.sql`
- `generate_invite_code()` now creates `4`-character uppercase alphanumeric invite codes
- legacy invite codes that do not match the new format are regenerated during the migration
- added `league_join_attempts` for per-user brute-force throttling on invalid invite-code submissions
- reinforced flat `SELECT USING (true)` policies on:
  `league_members`, `predictions`, `tournament_predictions`

### Server Actions

- `app/actions/league.ts` now includes:
  `createLeague`
  `joinLeague` with rate-limit guard
  `leaveLeague`
  `removeMember`
  `deleteLeague`
- owner-only actions now verify `auth.uid() === owner_id` server-side
- leave flow verifies the authenticated user matches the target user row before deleting membership

### UI / Routes

- `/game/leagues` now uses inline Create / Join panels instead of exposing both forms at all times
- invite sharing now uses a full share/copy link based on `/game/leagues?invite=CODE`
- `/game/leagues/[id]` now shows:
  leaderboard
  invite share card
  leave action for members
  remove-member and delete-league actions for owners
- `/game/predictions` now hides matches until both teams are resolved in the DB
- joker toggles now disable the rest of the current stage immediately in local UI state

### Corrections To Older Notes

- `/game` still redirects to `/game/predictions`, not `/game/leagues`
- `tournament_predictions.predicted_winner_team_id` is `uuid`, not `bigint`


## Phase 2.2 - League Route Stabilization + Predictions History

Date: 2026-04-22

This pass fixed a league-route regression and upgraded the match prediction UX into a historical feed.

### League Route Fix

- `/game/leagues/[id]/page.tsx` now loads the league via the admin client
- access is verified explicitly:
  owners are allowed
  members with a `league_members` row are allowed
  authenticated non-members are redirected back to `/game/leagues`
- this prevents false `404` screens right after `createLeague` redirects

### Joker Separation Fix

- joker bucket detection no longer relies on raw `stage === "group"`
- helpers now normalize the stage string first and only then classify:
  group-stage joker
  knockout joker
- optimistic UI now disables only the current bucket instead of the entire match list

### Predictions Feed Upgrade

- `/game/predictions` now renders:
  finished matches
  live matches
  scheduled matches
- future unresolved placeholders are still hidden until both team IDs exist
- finished matches are read-only and show:
  actual result
  user prediction
  `points_earned`
- exact score hits receive premium styling, and exact hits with a joker get the strongest highlight
- the page auto-scrolls to the first `live` or `scheduled` match on initial load


## Phase 2.3 - Joker Query Crash + Direct Join Flow

Date: 2026-04-22

This pass focused on the regressions that appeared after the previous game-hub rollout.

### Joker / Prediction Persistence Fix

- `lib/game/boosters.ts` now treats empty Supabase responses and missing-column situations as safe fallbacks instead of letting the layout fail
- `upsertMatchPrediction` now uses a defensive existing-prediction lookup with a legacy fallback when `is_joker_applied` is not queryable yet
- successful prediction saves now surface a clearer `✓ נשמר` state in the card UI

### Tournament Outrights State Fix

- `app/game/predictions/OutrightForm.tsx` was rewritten so winner and top-scorer state remain independent
- the form no longer refreshes the route after save, which avoids wiping local field state
- existing saved top-scorer values are now mapped back into the picker state when possible

### Direct League Join Flow

- new route: `/game/join/[code]`
- invite links now point directly to that route
- the join route:
  loads league metadata by invite code
  redirects unauthenticated users to login with the correct `next`
  lets an authenticated user confirm join
  redirects existing members straight into the league

### Layout Stability

- `app/game/loading.tsx` now provides a stable loading shell while switching between `My Predictions` and `My Leagues`
- `/game/leagues/page.tsx` now uses the admin client for membership and league list fetches to avoid user-RLS edge cases during navigation


## April 22, 2026 - Emergency RLS Recovery For Prediction Saves

This follow-up was required after a live regression where saving or loading predictions still triggered:

- `infinite recursion detected in policy for relation "league_members"`


## Phase 2.5 - Match Feed Cleanup + Outrights Lock Polish

Date: 2026-04-22

This pass focused on the final visual cleanup of `/game/predictions` and the league leaderboard outright rules.

### Predictions Feed

- removed the noisy helper counter that showed:
  `X matches shown / Y hidden`
- `LIVE` now uses a cyan broadcast treatment instead of any red styling
- scheduled cards with saved picks remain neutral-active and do not resemble a hit state

### Outrights Lock

- `loadPredictionsHubData()` now exposes `tournamentStarted`
- the lock condition is:
  first match status is no longer `scheduled`
  or
  first match kickoff time has passed
- `upsertTournamentPrediction()` now rejects writes once that condition is true
- `OutrightForm.tsx` now renders premium read-only badges instead of disabled controls after lock

### League Leaderboard

- leaderboard outright visibility is now:
  `hasTournamentStarted || member.user_id === currentAuthUserId`
- this means the authenticated user can always see their own winner / top-scorer picks in the private league table
- visible outright cells now render:
  team flag image + winner name
  player avatar icon + scorer name

### Opponent / Self View

- `/game/users/[id]` keeps the self-view bypass:
  if `viewerId === targetId`, render the full editable `PredictionsClient`
- opponent views still hide scheduled-match predictions, and now show the same premium outright badges when kickoff has already happened

### What changed

- `app/game/layout.tsx` now loads gamer-card profile data and joker usage through `createAdminClient()` after the user is authenticated
- `app/game/predictions/page.tsx` now loads:
  prediction history
  tournament outrights
  joker usage
  through the admin client as well
- `app/actions/predictions.ts` still authenticates with the user session first, but match lookup / prediction upsert / tournament upsert now execute through the admin client
- this removes the user-facing dependency on broken legacy RLS during prediction save and page refresh

### New migration

- added: `supabase/migrations/20260422000015_force_flat_prediction_rls.sql`
- purpose:
  drop every policy currently attached to `league_members`, `predictions`, and `tournament_predictions`
  recreate the intended flat authenticated policies from scratch

### What remains

- run `20260422000015_force_flat_prediction_rls.sql` in the live Supabase project
- once that migration is applied, the DB itself will stop emitting the recursive policy error even for user-scoped reads


## April 22, 2026 - Phase 2.1 Precision Polish

This pass focused on user-testing feedback around prediction readability and bracket clarity.

### Prediction Card Polish

- `app/game/predictions/MatchPredictionCard.tsx` was rewritten so:
  score formatting is always `home - away`
  score readouts render in an LTR context inside the RTL UI
  editable cards show a clear `הניחוש שלך: X - Y` block when a prediction already exists
- finished match cards now use four distinct visual outcomes:
  neutral gray for total miss
  light green for correct direction / wrong exact score
  green for regular exact hit
  diamond cyan-violet for exact hit + joker

### Search Box / Picker Behavior

- tournament-outright pickers now remount on successful save, which closes any open search dropdown and clears its transient filter state immediately after submission

### Leaderboard Avatar Fix

- league leaderboard avatars now enforce square circular crops with `object-cover` so uploaded portraits no longer stretch or squash

### Progressive Knockout Placeholders

- `lib/bracket/knockout.ts` now upgrades ambiguous best-third-place labels progressively
- if a specific group's third-place finisher is already locked, that segment is replaced with the real team name immediately
- unresolved groups remain as placeholder fragments such as `3C`
- bracket seed text now uses tighter truncation rules to avoid card-width blowups


## April 22, 2026 - Phase 2.2 Social View + Refined Prediction States

This pass focused on social visibility, exact-hit metrics, and a stricter result-color ladder.

### Prediction UX Refinements

- `app/game/predictions/OutrightForm.tsx`
  now uses a native `select` for top-scorer choice
- this removes the hanging custom autocomplete state after selection
- `app/game/predictions/MatchPredictionCard.tsx`
  now distinguishes:
  scheduled unsaved
  scheduled saved
  finished miss / no prediction
  finished direction hit
  finished exact hit
  finished exact + joker
- stage jokers now hide entirely on other matches once that stage joker is already consumed

### Gamer Card Stats

- `app/game/layout.tsx`
  now shows both `Total Score` and `Total Hits`
- `lib/game/stats.ts`
  computes exact-hit count from finished predictions

### Social Viewing

- leaderboard rows in `/game/leagues/[id]` are now clickable
- new route:
  `/game/users/[id]`
- server-side access is restricted to:
  self
  or users who share at least one league
- opponent pages show only `live` and `finished` matches
- scheduled matches are hidden completely
- leaderboard outright picks now stay locked until the tournament has officially started

### RLS Alignment

- added:
  `supabase/migrations/20260422000016_enable_social_prediction_selects.sql`
- this aligns:
  `profiles`
  `predictions`
  `tournament_predictions`
  with flat authenticated select access plus self-only writes

### Knockout Overflow Fix

- progressive third-place placeholders now prefer flag emoji output when a team's flag URL is known
- this keeps mixed labels compact enough for the bracket without expanding card width


## April 22, 2026 - Phase 2.3 QA Regression Fixes

What I changed in this pass:

- `app/game/predictions/MatchPredictionCard.tsx`
  now supports a true `isReadOnly` mode
- opponent cards no longer receive interactive handlers from the server tree, which fixes the React server/client crash on `/game/users/[id]`
- the score inputs now live inside the actual prediction form, so saved values like `4 - 1` no longer bounce back to `0 - 0`
- scheduled saved predictions now use a neutral dark blue / slate style instead of looking like a hit
- direction hits now use a soft yellow style and are determined strictly by outcome sign, including draw-vs-draw cases
- exact + joker hits now use a much heavier diamond / crown glow
- `app/game/predictions/OutrightForm.tsx`
  now renders winner and top-scorer with the same dark native select styling
- `/game/leagues`
  now shows:
  member count per league
  the authenticated user's current rank in that league
- `/game/users/[id]`
  now shows scheduled matches in locked mode instead of hiding them completely:
  `🔒 ? - ?`
- outright picks on both the opponent page and league leaderboard still stay hidden until the tournament starts
- `lib/bracket/knockout.ts` plus `app/dashboard/tournament/TournamentClient.tsx`
  now render known third-place placeholders as real image flags sourced from `team.logo_url`, not emoji flags

Validation:

- `npm run lint`
  passed
- `npm run build`
  passed

What still remains after this pass:

- run a quick real-device QA pass on the opponent page to confirm the scheduled lock state feels clear enough
- run the latest social-viewing migration on the live Supabase project if it has not been applied yet


## April 22, 2026 - Phase 2.4 UX Cleanup + Self-View Override

What I changed in this pass:

- `app/game/predictions/OutrightForm.tsx`
  moved back to polished custom dropdowns instead of native selects
- `components/pickers/TeamPicker.tsx`
  now renders real team flag images inside the winner dropdown
- `components/pickers/PlayerPicker.tsx`
  now renders a generic player avatar icon next to player names
- the helper sentence about winner-team players being sorted first was removed from the UI, while the sorting behavior stayed intact
- `app/game/predictions/PredictionsClient.tsx`
  no longer renders raw booster status text inside the predictions screen
- `app/game/predictions/MatchPredictionCard.tsx`
  now shows the saved prediction only once on scheduled cards, using a styled bet-slip badge
- `/game/users/[id]`
  now checks `viewerId === targetId`
  and if true, mounts the full editable `PredictionsClient` instead of the locked opponent view
- added dev-only route:
  `app/api/dev/predictions/reset/route.ts`
- `app/dev-tools/DevToolsClient.tsx`
  now includes the dangerous action:
  `איפוס כל הניחושים והג׳וקרים`
  which wipes `predictions` and `tournament_predictions`

Validation:

- `npm run lint`
  passed
- `npm run build`
  passed

What still remains after this pass:

- optional follow-up:
  decide later whether the dev reset should also clear the legacy mirror table `outright_bets`
- run one manual UX pass on the custom pickers to fine-tune spacing on the smallest mobile viewport


## April 22, 2026 - Phase 2.6 RTL + Opponent Context Fixes

What I changed in this pass:

- `app/game/predictions/MatchPredictionCard.tsx`
  now forces all rendered score strings through LTR wrappers so `home - away` no longer flips visually in RTL
- the exact-hit-plus-Joker treatment was re-skinned into a deep purple / magenta glow that matches the booster cards
- `app/game/layout.tsx`
  now mounts a route-aware `components/game/GameHeroShell.tsx`
- the main gamer header still appears on the player's own game surfaces
- the Joker inventory is now shown only on `/game/predictions`
- `/game/leagues` keeps the header stats but no booster cards
- `/game/users/[id]` no longer shows the authenticated viewer's header at all
- `app/game/users/[id]/page.tsx`
  now renders the target opponent's own:
  avatar
  display name
  total score
  total hits
- opponent outright badges now use a larger `hero` size for better readability
- `components/game/OutrightChoiceBadge.tsx`
  now supports a larger `hero` variant for locked display cards
- `app/dashboard/tournament/page.tsx`
  now passes live score context for each currently playing group-stage team
- `app/dashboard/tournament/TournamentClient.tsx`
  now replaces the generic live badge with the actual live score for each team row
- live group score badges are now color-coded:
  green when that row's team is winning
  amber when drawing
  red when losing
- `app/dev-tools/DevToolsClient.tsx`
  now forces the editable score clusters into LTR so the numbers do not visually swap in Hebrew layout

Validation:

- `npm run lint`
  passed
- `npm run build`
  passed

What still remains after this pass:

- do one real-device QA sweep on `/game/users/[id]` to confirm the larger outright badges feel balanced on narrow phones
- visually verify the new live-score pills in a real `status = live` group match against RTL spacing in production data


## April 22, 2026 - Phase 2.7 RTL Entry Hardening + Live Prediction Tracking

What I changed in this pass:

- `app/dev-tools/DevToolsClient.tsx`
  now forces score-entry clusters into explicit LTR layout with `Home` / `Away` labels above each numeric input
- this hardens both:
  visual order
  semantic binding to `home_score`, `away_score`, `home_penalty_score`, `away_penalty_score`
- `app/game/predictions/MatchPredictionCard.tsx`
  now applies live comparison styling to the user's own prediction box during `status = live`
- live prediction box logic is now:
  green if the current live score exactly matches the prediction
  yellow if the current result direction matches
  red if it currently misses
- the cyan live broadcast pill at the top of the card remains unchanged
- `app/dashboard/tournament/TournamentClient.tsx`
  removed the `GF / ז+` column from group tables to avoid overflow
- team-name rows now use tighter truncation so long names do not wrap under the live-score chip
- knockout score fragments continue to render in explicit LTR wrappers
- `app/game/predictions/OutrightForm.tsx`
  was simplified into a lighter premium layout
- `components/pickers/TeamPicker.tsx`
  and `components/pickers/PlayerPicker.tsx`
  now present editable outrights like sleek badge-dropdowns rather than bulky form controls

Validation:

- `npm run lint`
  passed
- `npm run build`
  passed

What still remains after this pass:

- do one manual QA pass on the dev-tools grid with real Hebrew team names to confirm the new Home/Away score clusters feel unambiguous
- visually confirm the new live prediction-border states against a real in-progress match once live data is available
